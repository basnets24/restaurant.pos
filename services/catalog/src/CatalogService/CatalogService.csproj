<Project Sdk="Microsoft.NET.Sdk.Web">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <RootNamespace>CatalogService</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Common.Library" Version="1.0.23" />
        <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.10" />
        <PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="10.0.10">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.3" />
        <PackageReference Include="Swashbuckle.AspNetCore" Version="10.2.3" />
    </ItemGroup>

    <!-- TODO(modifier-authoring): flip back to <PackageReference Include="Messaging.Contracts" Version="1.0.8" />
         at merge. 1.0.8 (MenuItemModifiersChanged) only publishes on a push to dev/main, so this
         branch consumes it as a project reference in the meantime - same gate order/payment are
         already carrying for 1.0.7's CustomerId addition. Confirm 1.0.8 is on the feed (version
         index, not `dotnet package search`) before flipping back. -->
    <ItemGroup>
        <ProjectReference Include="..\..\..\..\shared\Messaging.Contracts\Messaging.Contracts.csproj" />
    </ItemGroup>

</Project>
