<Project Sdk="Microsoft.NET.Sdk.Web">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
      <UserSecretsId>c671bfe8-99f5-4559-80c4-4b3a09de2a31</UserSecretsId>
  </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Common.Library" Version="1.0.23" />
        <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.10" />
        <PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="10.0.10">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.3" />
        <PackageReference Include="Stripe.net" Version="52.1.1" />
        <PackageReference Include="Swashbuckle.AspNetCore" Version="10.2.3" />
    </ItemGroup>

    <!-- TODO(diner-ordering): flip back to <PackageReference Include="Messaging.Contracts" Version="1.0.7" />
         at merge. 1.0.7 (PaymentRequested.CustomerId) only publishes on a push to dev/main, so
         this branch consumes it as a project reference. Do not merge without reverting this and
         confirming 1.0.7 is actually on the feed - the publish workflow does not bump versions
         and skip-duplicate fails silently. -->
    <ItemGroup>
        <ProjectReference Include="..\..\..\shared\messaging.contracts\Messaging.Contracts.csproj" />
    </ItemGroup>

</Project>